# a terminal is needed to run tsc watch mode (tsc --watch), this compiles automatically
# another terminal to run compiled js file (node index.js)
# check on the browser (or postman) http://localhost:3000/